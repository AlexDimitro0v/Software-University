function solution(input) {
    let text = input[0];

    for (let i=0; i<text.length; i++){
        let letter = text[i]
        console.log(letter);
    }
    // // Returns the element
    // for (const i of text) {
    //     console.log(i)
    // }

    // // Returns the index
    // for (const i in text) {
    //     console.log(i, text[i])
    // }

}

solution(["alex"]);